# Reference Assets

This directory contains reference data used by the design documents.

- `ontology/`: historical target, condition, action, severity, and tag vocabularies.
- `verification/`: reference mappings for compiler warnings, clang-tidy checks,
  sanitizers, and review checks.

These files are not loaded by the current runtime path. They are kept as design
references for future policy and verification work.
